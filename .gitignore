# 環境變數與機密金鑰
.env
upload_secrets.sh
*.json
!package.json
!data-verification-ui/package-lock.json
!assets_config.json
!docs/oss_candidates/*.json
!tests/fixtures/reports/**/expected_validation.json
!tests/fixtures/mock_data/*.json
!fixtures/*.json
!data/compute_memory_mock.json
!data/onchain_metrics_mock.json
*-credentials.json
service-account*.json
gcp-key*.json

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/

# macOS
.DS_Store

# IDE
.idea/
.vscode/

# gstack：本機透過 ~/.claude/skills 或專案 .agents/skills 安裝（含機器絕對路徑 symlink，勿提交）
.agents/

# Test
.pytest_cache/
.hypothesis/

# Local generated artifacts
logs/
daily_chart.png
portfolio_holdings.jsonl
price_alerts.jsonl
# Earnings insight live scaffold (queue 45 P3) — copy
# data/deep_filing_analysis.example.jsonl to data/deep_filing_analysis.jsonl
# and edit with real NotebookLM/agency output. The .example file is committed
# as a structural template; the live file stays out of git.
data/deep_filing_analysis.jsonl
# .claude：僅追蹤設定、hooks、rules；其餘（如本機 agents）仍忽略
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/hooks/**
!.claude/rules/
!.claude/rules/**

# Run scratchpad (Phase 1: tool/gate trace JSONL — may contain sensitive snippets)
.qsilicon/scratchpad/
# Last gate validation failure (draft report + full issues list)
.qsilicon/last_gate_failure/
.pytest_gate_fail_tmp/
.gstack/
