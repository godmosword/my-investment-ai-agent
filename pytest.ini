[pytest]
asyncio_default_fixture_loop_scope = function
markers =
    smoke: fast critical checks for PR quick path
    boundary: edge-case / contract tests (HTTP, gates, dual-crew failure paths)
    slow: property-based or heavier checks; optional to exclude locally
