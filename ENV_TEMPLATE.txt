# ============================================================
# Q-Silicon 日報 Pipeline — 環境變數範本
# 將此檔內容複製到專案根目錄的 .env，填入實際金鑰後存檔
# ============================================================

# ---------- 必要：LLM API（缺一則啟動時報錯）----------
XAI_API_KEY=
OPENROUTER_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
# Anthropic（crew LLM fallback 鏈會用；缺則僅跳過 Claude 備援）
ANTHROPIC_API_KEY=

# ---------- 開發／離線測試（MOCK_APIS；見 docs/ADR_OFFICE_HOURS_TOOLS_PLATFORM.md）----------
# MOCK_APIS=1 — 啟用 tools.base.load_mock_json → tests/fixtures/mock_data/*.json；各 @tool 的 mock 分支將逐步接入 tools_legacy
# SKIP_EQUITY_YF_BACKFILL=1 — assemble 階段不以 yfinance 補美股 trade_legs 現價／進場或 R:R 機械推算目標停損（離線／除錯）

# ---------- 選填：LiteLLM 模型 ID（見 config.py 預設；改設定免動 crew.py）----------
# MODEL_GROK=xai/grok-4-1-fast-reasoning
# OPENAI_MODEL=openai/gpt-4o-mini
# MODEL_GPT=openai/gpt-4o-mini
# MODEL_GEMINI=gemini/gemini-2.5-pro
# MODEL_CLAUDE=anthropic/claude-sonnet-4-20250514

# ---------- 省 LLM token／延遲（營運槓桿；不影響程式預設，按需開啟）----------
# 優先順序建議：① 關閉非必要額外呼叫 ② 降階模型 ③ 再考慮改 prompt（須過 Gate／品質審閱）
# · REPORT_LLM_JUDGE=0（預設）— 設 1 會多一輪結構後 rubric LLM
# · EDITOR_AGENT_ENABLED=0（預設）— 設 1 會多一輪潤稿 LLM
# · PIPELINE_SKIP_SENTIMENT_SCORE=1 — 加密段略過 sentiment_score_tool（少一輪工具／LLM；見下方同名變數）
# · COMPANY_CREW_ENABLED=0 — Growth 試點會加長 exclusion context；非必要勿開
# · MODEL_GROK／MODEL_GPT／MODEL_GEMINI／MODEL_CLAUDE — 改較小／較快 ID 可降費；日報品質與 Gate 通過率須自行觀察
# · CREW_DISABLE_ASYNC_RESEARCH=1 — 單一巨型研究 task vs 多子任務；不保證總 token 下降（單次 context 可能更大），僅宜除錯／實驗

# ---------- 資料/搜尋 API（工具會用到，缺則該工具回傳 DATA_MISSING）----------
# CoinGlass API v4：Base open-api-v4.coinglass.com，Header CG-API-KEY。若 JSON 回 code=401、msg=Upgrade plan＝方案不含該端點（非 URL 錯），見 README / AGENTS.md
COINGLASS_API_KEY=
CRYPTOPANIC_API_KEY=
# POLYGON_API_KEY：polygon.io — options snapshots/Greeks/trades（options flow + GEX 管線）
# 需付費 Options 方案：Starter 拿 Greeks→GEX；Advanced 拿 trades→tick sweep/block
# 缺 key 或方案不足 → pipeline 回 [DATA_MISSING:polygon_options_*]，不捏造數據
POLYGON_API_KEY=
# Options 管線 BigQuery 歷史表（可選；未設＝該表略過寫入）。DDL 見 docs/SQL/options_*.sql
# 診斷：python scripts/verify_bq_tables.py
# Cloud Run deploy：Secret Manager 未建 POLYGON_API_KEY 時會略過掛載並讓前端顯示 pending，不阻塞主日報部署
# OPTIONS_SNAPSHOTS_TABLE=PROJECT.market_data.options_snapshots
# OPTIONS_UNUSUAL_TRADES_TABLE=PROJECT.market_data.options_unusual_trades
# OPTIONS_GEX_HISTORY_TABLE=PROJECT.market_data.options_gex_history
# OPTIONS_GEX_BY_STRIKE_TABLE=PROJECT.market_data.options_gex_by_strike   # by-strike 柱狀圖用
# NEWSAPI_KEY：newsapi.org — Bloomberg/Reuters/CNBC 主流財經新聞，每月 1000 次免費
# 申請：https://newsapi.org/register
NEWSAPI_KEY=
# GNEWS_API_KEY：gnews.io — 多語言新聞搜尋，每日 100 次免費
# 申請：https://gnews.io/
GNEWS_API_KEY=
# APIFY_API_TOKEN：Apify 搜尋引擎（市場搜尋/傳聞/OpenRouter 熱度），已降為最後 fallback
APIFY_API_TOKEN=

# ---------- P2 新增：鏈上數據 API（選填，缺則該工具回傳 DATA_MISSING）----------
# CRYPTOQUANT_API_KEY：CryptoQuant API — SOPR、交易所 BTC 淨流向
# 申請：https://cryptoquant.com/  （免費方案有限速）
CRYPTOQUANT_API_KEY=
# GLASSNODE_API_KEY：Glassnode API — NUPL 未實現損益比
# 申請：https://glassnode.com/  （免費方案有延遲）
GLASSNODE_API_KEY=
# CoinGecko / Alternative.me / Blockchain.com：隊列 52 F0 已登錄治理；既有 legacy 工具使用 public endpoints。
# 若後續新增 Portal live fetcher，CoinGecko Demo API key 預留名稱：COINGECKO_DEMO_API_KEY（目前未被程式讀取）。
# 注意：Alternative.me Fear & Greed、Blockchain.com charts 無需額外金鑰；DefiLlama 仍為 pending 候選來源。
# FRED_API_KEY：美債 DGS2 等 fallback（macro_context_tool）
FRED_API_KEY=
# FMP_API_KEY：可選，/api/macro/snapshot 的 Next Fed/CPI catalyst calendar
FMP_API_KEY=
# FINANCIAL_DATASETS_API_KEY：financialdatasets.ai 美股損益／資產負債／現金流（AI 段基本面）；免費層常含 AAPL/NVDA/MSFT
# FINANCIAL_DATASETS_API_BASE=https://api.financialdatasets.ai
FINANCIAL_DATASETS_API_KEY=

# ---------- Compute / Memory dashboard live sources（queue 45 P2-live）----------
# SEC_EDGAR_CONTACT_EMAIL：SEC EDGAR API User-Agent 識別（SEC fair-use 政策要求）
SEC_EDGAR_CONTACT_EMAIL=
# COMPUTE_MEMORY_CAPEX_LIVE：1=啟用 SEC EDGAR 10-Q capex live fetch；0=固定 mock fixture
COMPUTE_MEMORY_CAPEX_LIVE=0
# COMPUTE_MEMORY_GPU_LIVE：1=啟用 CoreWeave public pricing 解析（GPU spot）；0=固定 mock fixture
COMPUTE_MEMORY_GPU_LIVE=0

# ---------- Telegram 推送（選填，不填則不發送）----------
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# TELEGRAM_REPORT_HISTORY_URL=https://your-pwa.example/archive  # 非空時首則戰報附「查看歷史」inline 連結
# REPORT_PROFILE=full   # 日報 Telegram 版型：`full`（預設，與 Phase 0 **byte-identical**）｜`lite`（精簡：無上期／宏觀／預測市場／儀表板與新聞／呢喃／機構速讀；僅市場模式 + 雙邊區塊④ + QSREC 尾段）｜`crypto-only`（僅加密前段 + 區塊①–④ + 尾段；無 AI 全段／機構速讀；Gate 見 `report_html_gates.py`）。亦驅動 `validate_report(..., profile=)`（`lite`／`crypto-only` 放寬四區塊／新聞與美股相關 Gate；機構 Phase A/B/C **HTML** 不檢；見 `report_html_gates.py`）。
# BRIEF_CURRENT_AFFAIRS=0   # Phase 5：1=在 `full` 版型【機構速讀】與尾段之間插入可選〔時事多觀點〕（須 `DailyBriefReport.current_affairs_roundtable` 結構化非空；預設 0 **不渲染**，與 Phase 0 **byte-identical**）。
# CURRENT_AFFAIRS_MIN_VOICES=2
# CURRENT_AFFAIRS_MAX_VOICES=4
# STRICT_CURRENT_AFFAIRS_ROUNDTABLE_GATE=0   # 1=在 `full` 且 `BRIEF_CURRENT_AFFAIRS=1` 時強檢〔時事多觀點〕（HTML + 建議管線傳 `structured_report` 做結構化交叉檢；見 `report_html_gates.py`／`main.py`）。
# BRIEF_CURRENT_AFFAIRS_JSON=   # 可選；單行 JSON 覆寫 `CurrentAffairsRoundtable`（crew 失敗或本機測試；須 `BRIEF_CURRENT_AFFAIRS=1`）。

# Tech pulse（可選；僅併入 exclude_context，不寫入 price_context）— 見 `docs/ADR_TECH_PULSE_INTEGRATION.md`
# TECH_PULSE_IN_BRIEF=0   # 1=在日報 Crew 的 exclusion 尾段附加 tech_pulse 摘要（預設 0）。
# TECH_PULSE_URL=   # HTTPS GET；JSON 建議含 `summary` 字串；缺或 `MOCK_APIS=1` 時見 `tools/tech_pulse_tool.py`。
# TECH_PULSE_FIRESTORE_COLLECTION=tech_pulse_memory_items  # /api/news digest/deep/themes 的 Firestore collection
# TECH_PULSE_FIRESTORE_PROJECT=   # 可選；未設時使用 google-cloud-firestore 預設 credentials/project
# BRIEF_DYNAMIC_RENDER=0   # 1=當 `BRIEF_LAYOUT_FILE` 使 `profile_block_ids("full")` 與內建順序不同時，`full` 走 macro 動態串接（預設 0 維持 Phase 0 **byte-identical**）。
# Staging：`BRIEF_DYNAMIC_RENDER=1` 前請在非生產跑 `validate_report`／`scripts/verify_graph_gate.sh` 與 `test_dynamic_full_render.py`；確認 `STRICT_*` Gate 與 Telegram 白名單無誤擋。風險：與靜態 `telegram_full.j2` 路徑分歧，回歸須比對 HTML 區塊順序與錨點。
# STRICT_LITE_EXEC_SUMMARY_PASS6_GATE=0   # 1=`lite` 執行摘要「→」要點 ≤3 條、每條 ≤40 字（見 `report_html_gates.py`）。
# BRIEF_LAYOUT_FILE=   # 可選；指向 YAML，與內建 `PROFILES` merge 粗粒度 **block 順序**（白名單、同集合重排）。專案根相對或絕對路徑。見 `config/brief_layouts/README.md`。
# DAILY_BRIEF_JSON_DIR=   # 可選；管線成功 assemble 後寫 `YYYY-MM-DD.json`（`DailyBriefReport` 之 JSON）；未設則寫 `.qsilicon/daily_brief_reports/{date}.json`，供 `GET /api/reports/{date}/structured` 填充 **`daily_brief_report`**。亦會嘗試 **`logs/run_YYYYMMDD_*/raw_data.json`**。見根目錄 `DESIGN.md`、`docs/DASHBOARD_CONTRACT.md`。
# DAILY_BRIEF_JSON_BQ_TABLE=   # 可選；非空且未 `SKIP_BIGQUERY` 時，額外寫完整 DailyBriefReport JSON row。DDL 見 docs/SQL/daily_brief_report_json.sql。

# ---------- GCP / BigQuery（選填，預設專案名見下）----------
GCP_PROJECT_ID=my-investment-ai-agent

# ---------- 資產清單（選填）----------
# ASSETS_CONFIG_PATH=  # 覆寫 assets_config.json 路徑；影響 tracker（crypto）、financial_datasets_tool「watchlist」批次、ai_sector_market_tool yfinance 籃

# ---------- 選填：除錯與行為 ----------
# LOG_LEVEL=INFO
# DEBUG=0
# CREW_VERBOSE=0
# CREW_DISABLE_ASYNC_RESEARCH=0  # 1=Crypto／AI 研究員回退單一巨型 Task（關閉 async_execution 子任務拆分；限流除錯用）
# SKIP_TELEGRAM=0
# SKIP_BIGQUERY=0
# PIPELINE_STRICT_ENV=0   # 1=生產／排程硬擋：未 SKIP_TELEGRAM 須 TELEGRAM_BOT_TOKEN+CHAT_ID；未 SKIP_BIGQUERY 須 GCP_PROJECT_ID 與 GCP_SA_KEY 或 GOOGLE_APPLICATION_CREDENTIALS
# STRICT_NEWS_FRESHNESS_GATE=0   # 排程／生產建議=1（見 docs/DEPLOY_RUNBOOK.md）；1=新聞時間戳須落在報告時間前 NEWS_FRESHNESS_WINDOW_HOURS 小時內
# NEWS_FRESHNESS_WINDOW_HOURS=36   # 預設 36h（與 crew 新聞新鮮度一致）；可改 48
# NEWS_FRESHNESS_SOURCE_WHITELIST=FRED,IMF   # 行內含關鍵字則略過該則新鮮度檢查（逗號分隔，大小寫不敏感）
# PIPELINE_REPORT_DATE=   # YYYY-MM-DD：新聞新鮮度以該日 HKT 23:59:59 為參考；並注入 crew exclusion 開頭
# PREDICTION_MARKETS_IN_BRIEF=0  # 1=在 assemble 注入【預測市場熱門】；預設 0（Polymarket Gamma 公開 API，無金鑰，手動研究／staging 用）
# PREDICTION_MARKETS_TAG_IDS=   # 逗號分隔 Gamma tag_id（GET https://gamma-api.polymarket.com/tags 查可用標籤）；可縮小主題至政治／總經等
# PREDICTION_MARKETS_EXCLUDE_TAG_IDS=  # 逗號分隔要排除的 tag_id（請求僅帶第一個 exclude_tag_id）
# PREDICTION_MARKETS_KEYWORDS=   # 逗號分隔：題目含關鍵字優先；不足 3 條再併全域成交量
# PREDICTION_MARKETS_DENYLIST=nba,nfl,rebounds,assists,touchdown
# EARNINGS_FOCUS_MODE=0   # 1 或 auto：當錨定日（PIPELINE_REPORT_DATE 或當日 UTC）有 watchlist 美股 yfinance 財報日曆命中時，於 exclusion 注入【財報聚焦日】（含盤前／盤後敘述規則）並要求 AI 段 financial_datasets_tool(TICKER:quarterly) 與財報主線新聞（見 earnings_focus.py）。錨定日為**週五／週六／週日**時，**無論本開關**，皆會注入【下週財報預告】（下一完整曆週 Mon–Sun）。讀者版 AI 儀表板另由 pipeline 產生【財報雷達｜未來 7 天】事件預告；不含 EPS／營收共識 forecast。
# REPORT_COMPARE_MODE=0   # Phase 3：1=每次 validate 後額外跑候選路徑並 log 差異（不切 Telegram/BQ 決策）
# USE_LANGGRAPH_ENGINE=0  # Phase 3：1=啟用 LangGraph 狀態機（預設 0 走既有 CrewAI）
# GRAPH_ENABLE_TOOL_CALLS=1  # LangGraph 節點是否呼叫工具；測試可設 0
# GRAPH_LLM_TRADE_PICKER=0  # 1=啟用 trade_picker 節點以結構化 LLM 產出 proposed_trades（0~2 筆，無價格）；risk_off 時仍為空。需 OPENAI_API_KEY 與 MODEL_GPT 所用供應商。
# GRAPH_LLM_REVIEWER=0  # 1=trade_picker 有 proposed_trades 時啟用 LangGraph reviewer loop：Python 先擋 schema/ticker，LLM 僅查 thesis/direction/news 邏輯矛盾；hard cap=2，降級保留最後版本。需 GRAPH_LLM_TRADE_PICKER=1。
# GRAPH_LLM_TRADE_REVIEWER=0  # 同上；較明確的 alias，任一設為 1 即啟用。
# GRAPH_DEEP_RESEARCH_TOOL_LLM=0  # 1=深度查證節點以 bind_tools 真實呼叫 RESEARCH_TOOLS（需 API 金鑰與 LLM）
# LANGGRAPH_SKIP_FORMATTER_CREW=0  # 1=Final_Formatter 不跑既有 Formatter Crew；改以 LangGraph 內 slim 結構化輸出 + 決定性組裝 CryptoSection/AISection（dashboard 自 raw_data；新聞／交易由 news_scraper／trade_picker 等節點填入 raw_news、proposed_trades 後一併組裝）。需 OPENAI_API_KEY 與 Formatter 所用模型。
# MAX_REPORT_RETRIES=2
# MAX_503_RETRIES=3
# BACKOFF_BASE_SEC=25
# VERIFY_API_KEYS=1   # 啟動後對 NewsAPI、Apify 做輕量連線探測；另見日誌「API key inventory」
# NEWSAPI_DAILY_CALL_LIMIT=120
# GNEWS_DAILY_CALL_LIMIT=120
# APIFY_DAILY_CALL_LIMIT=30
# DISABLE_SOURCE_HEALTH_BQ=1
# ALLOW_PARTIAL_NEWS_GATE=1   # 預設開啟：符合「新聞資料不足分段」時允許 3~5 則〔新聞 N〕放行（仍須〔新聞1~3〕+ UTC+8 + 不補虛構宣告）；設 0 強制永遠要 6 則
# STRICT_PICK_JUSTIFICATION=1 # 預設開啟：驗證「本日選擇理由」是否含足夠催化/鏈上或退階說明並點名 QSREC 標的（幣/股分開）；設 0 關閉
# STRICT_PICK_ROTATION=1   # 預設開啟：若今日 QSREC 與昨日 BQ 已存 canonical 標的集合完全相同，需通過同標覆核（理由+分差）；設 0 關閉
# STRICT_PICK_SCORING=1    # 預設開啟：QSREC 每筆需填 selection/catalyst/flow/technical/risk_fit/execution 分數（0~100）+ alt_candidate_score + score_gap
# QSREC_ALLOW_OPPOSING_DIRECTIONS=0  # 預設關閉：設 1 時略過「同 category+asset 不得同時 LONG 與 SHORT」機檢（僅限明確知悉對沖語義時使用）
# ALLOW_REPEAT_PICK_OVERRIDE=1  # 預設開啟：允許同標延續覆核（關閉後強制至少換一檔/一腿）
# AUTO_REPEAT_PICK_DISCLAIMER=1   # 預設開啟：與昨日 BQ 紀錄標的相同且允許覆核時，若理由缺輪動認可片語（如「連日維持…」「重複選用理由」等），assemble 前自動前綴讀者向補註（設 0 關閉）；前綴不含 pipeline／BQ 內部字樣
# PICK_ROTATION_OVERRIDE_MIN_GAP=12  # 同標延續最低分差（score_gap）門檻
# PICK_REPEAT_DAYS_MAX=2   # 同標延續時，至少 1 筆需滿足 repeat_days <= 此值
# PICK_REPEAT_MIN_SELECTION_SCORE=75  # 同標延續時，至少 1 筆需滿足 selection_score >= 此值
# ADAPTIVE_GATE_THRESHOLDS=0   # 1=啟用 gate_failure_log BQ 讀取並可能上調 PICK_ROTATION_OVERRIDE_MIN_GAP（見 adaptive_gate_thresholds.py）
# ADAPTIVE_GATE_BQ_READ=1      # 0=僅用環境預設 gap，不查 BQ（CI／無憑證時可關）
# ADAPTIVE_GATE_BQ_LOOKBACK_DAYS=14
# ADAPTIVE_BQ_MIN_FAILURE_ROWS=5   # BQ 內失敗列數低於此則不調整
# ADAPTIVE_ROTATION_PREVIEW_FRACTION=0.35  # issues_preview 疑似 rotation／選標 占比 ≥ 此值才 bump
# ADAPTIVE_GATE_GAP_BUMP=2     # 滿足條件時在環境 gap 上加的數值
# ADAPTIVE_GATE_GAP_CEILING=24 # 有效 gap 上限（0=不限制）
# PICK_ROLLING_FREQ_GATE=0   # 1=啟用滾動視窗同資產出現日數上限（見 docs/PICK_ROTATION_SEMANTICS.md）
# PICK_ROLLING_WINDOW_DAYS=5
# PICK_ROLLING_MAX_DISTINCT_DAYS=0  # 0=關閉；例如 3=視窗內已出現 ≥3 個相異交易日則擋本日再選
# DATA_MISSING_COUNT_GATE_MAX=0     # 0=關閉；>0 時正文 [DATA_MISSING:…] 個數超過則 blocking
# STRICT_EXEC_SUMMARY_HTML_GATE=0   # 1=HTML 須含【執行摘要】且至少 2 條要點（·／→ 等）
# STRICT_INSTITUTIONAL_PHASE_A_GATE=0  # 1=HTML 須含機構免責 blockquote、【投資命題】、支持/反駁各≥3 條·、【關鍵假設】2–4 條、【敘事失效】（blocking）；預設關閉
# 結構化契約：同上開關=1 時 DailyBriefReport 亦要求 CryptoSection 填滿 investment_thesis_one_liner 等欄位（見 schemas._institutional_phase_a_structured_issues）
# STRICT_INSTITUTIONAL_PHASE_B_GATE=0  # 1=HTML 須含【組合與曝險框架】、【三情境機率】（三行·、機率合計100%）、〔新聞 1–6〕各行「市場定價：」+ 三擇一字面；結構化同時要求 portfolio_framing_summary、scenario_probability_notes、每則 NewsItem.pricing_note
# STRICT_INSTITUTIONAL_PHASE_C_GATE=0  # 1=HTML 須含【加密週期與估值錨】、【美股估值與修正框架】、【近端事件日曆】3–6 條·（每條含日期）、可執行腿各附「流動性／執行」；結構化要求 crypto_cycle_valuation_notes、equity_valuation_framing、event_calendar_lines、trade_legs.liquidity_execution_note
# MIN_TOOL_CALLS_PER_PIPELINE=0     # >0 且本輪 traced 工具呼叫低於此值則拋錯重試（建議 SCRATCHPAD_ENABLED=1）
# STRICT_PAIR_TRADE_UNIT_GATE=0     # 1=配對交易單位機檢升格為 blocking
# STRICT_MACRO_CONFLICT_GATE=0      # 1=宏觀 2Y/利差矛盾升格 blocking
# STRICT_TOOL_EVIDENCE_GATE=0       # 1=加密段工具關鍵詞密度不足則 blocking
# STRICT_TOOL_EVIDENCE_MIN_HITS=3
# STRICT_QSREC_SCENARIO_GATE=0      # 1=QSREC confidence≥3 須含 bull/base/bear_scenario（HTML JSON 路徑）
# SCRATCHPAD_ENABLED=1     # Phase 1：每次產報寫入 .qsilicon/scratchpad/*.jsonl（init/gate/tool）；設 0 關閉
# SCRATCHPAD_DIR=.qsilicon/scratchpad  # scratchpad 目錄（相對專案根）
# EQUITY_BACKFILL_SCRATCHPAD_LOG=1   # assemble 觸發美股 yfinance／R:R 備援時寫 scratchpad `equity_price_backfill`（設 0 關閉）
# MAX_TOOL_CALLS_PER_RUN=80   # 單次產報 run 內 traced_tool_execution 上限；0=不限制
# REPEATED_CALL_THRESHOLD=3    # 連續相同 tool+參數次數達此值則中止；0=關閉
# CREW_FUTURE_TIMEOUT_SEC=2400 # 雙 Crew 單軌 future.result 逾時（秒）；預設 40m，對齊慢 LLM／多工具
# PIPELINE_HARD_DEADLINE_SEC=13200 # 整段產報牆鐘預算（秒）；預設 3h40m，對齊 4h Cloud Run Job
# PIPELINE_SKIP_SENTIMENT_SCORE=0 # 1=加密研究員不掛 sentiment_score_tool（省一輪 LLM；BQ sentiment_score 可能為空）
# STRICT_AI_FUNDAMENTALS_CITATION=0  # 1=AI「本日選擇理由」含基本面用語時須在 AI 段出現 FinancialDatasets 標記
# STRICT_INVESTMENT_DASHBOARD_NUMERIC_GATE=0  # 1=每則投資解讀之數字錨點須出現在同段區塊①儀表板 <code> 讀值（blocking）；預設關閉；觀望模式略過
# STRICT_CHATTER_MSM_VERIFY_GATE=0  # 1=區塊③呢喃含「可信度」之條目須同列「主流媒體二次驗證：是/否」（blocking）；預設關閉
# SKIP_BTC_MA_DASHBOARD_INJECT=0   # 1=組裝階段不注入 BTC MA20/MA50（yfinance 日線）；預設注入（MOCK_APIS=1 時略過）
# REPORT_LLM_JUDGE=0           # 1=產線結構驗證後呼叫 LLM rubric 評分（寫入 scratchpad judge_result）
# REPORT_LLM_JUDGE_MODEL=openai/gpt-4o-mini
# REPORT_LLM_JUDGE_MIN_SCORE=70
# REPORT_LLM_JUDGE_BLOCKING=0  # 1=評分未過則視同結構驗證失敗並重試
# ---------- 日報品質代理（validate 乾淨通過或 warn-pass 交付後；不阻擋發報）----------
# REPORT_QUALITY_AGENT=0              # 1=啟用：LLM rubric = report_judge.llm_quality_judge；模型同 **REPORT_LLM_JUDGE_MODEL**（未設時程式預設 **openai/gpt-4o-mini**，與上節一致；固定用 gpt-4o-mini 可不覆寫）
# REPORT_QUALITY_AGENT_COMPOSITE_MIN=72   # 複合分低於此則寫入 TODOS.md 機器區塊（改善項）
# REPORT_QUALITY_AGENT_SOURCE=dual    # dual | llm | domain（dual=LLM+domain 平均；缺 OPENAI_API_KEY 時自動退為 domain）
# REPORT_QUALITY_AGENT_DOMAIN=0       # 1=複合分納入 report_judge.domain_quality_check
# REPORT_QUALITY_AGENT_TODOS_PATH=    # 預設專案根 TODOS.md
# REPORT_QUALITY_AGENT_TODOS_MAX_LINES=40  # 機器區塊內最多保留幾條 bullet（自頂裁切）
# REPORT_QUALITY_AGENT_GIT_PUSH=0     # 1=寫入 TODOS 後 git commit + push（須 CI/權限；預設關）
# REPORT_QUALITY_AGENT_GIT_ALLOW=0    # 與 GIT_PUSH 同開才執行（雙重保險，避免誤推）
# GATE_FAILURE_ARTIFACTS=1   # validate 失敗時寫入 .qsilicon/last_gate_failure/（draft_report.txt、issues.txt、validation_summary.json）
# GATE_FAILURE_ARTIFACT_DIR=   # 自訂目錄（預設專案根下 .qsilicon/last_gate_failure）
# GATE_FAILURE_BQ_LOG=1      # 1=管線每次 validate 含 issues 時寫入 BigQuery `market_data.gate_failure_log`（結構化分類 + profile；不自動改 prompt）；SKIP_BIGQUERY=1 時仍略過。`llm_run_log` 寫入亦含 profile（Phase 4c）
# REVIEWER_LOG_BQ=1          # 1=LangGraph trade reviewer loop 寫入 BigQuery `market_data.reviewer_log`（profile/track/revision_count/fail reasons/degraded/latency）；SKIP_BIGQUERY=1 時仍略過。DDL 見 docs/SQL/reviewer_log.sql
# GATE_ALERT_FULL_ISSUES=1   # Gate 告警後再發 1+ 則純文字訊息列出完整 issues（設 0 只保留 top 3 摘要行）
# GATE_FAILURE_DIGEST_DAYS=14  # scripts/gate_failure_hint_digest.py 查詢近 N 日（1~90）
# GATE_FAILURE_DIGEST_OUT=.qsilicon/gate_failure_digest.md  # digest script 可選輸出路徑

# ---------- 路線圖：潤稿／權重 context／Company 試點 ----------
# EDITOR_AGENT_ENABLED=0     # 1=assemble+render 後、validate 前跑潤稿（預設關）
# EDITOR_AGENT_MODEL=openai/gpt-4o-mini
# WEIGHTS_CONTEXT_ENABLED=0  # 1=fetch_exclusion_context 附加 ML 權重摘要（見 signal_weights_store）
# SIGNAL_WEIGHTS_DIR=.qsilicon
# COMPANY_CREW_ENABLED=0     # 1=main 啟動時 prepend Growth 敘事至 exclusion context；寫入 .qsilicon/company_run_latest.json
# SHADOW_BENCHMARK_LOG=0     # 1=main 記錄 dual-crew / company 試點耗時與新聞量到 JSONL（供切片 4 量測）
# SHADOW_BENCHMARK_PATH=.qsilicon/crew_shadow_benchmark.jsonl

# ---------- OSS Scout（本機／weekly-scout workflow；見 docs/oss_candidates/README.md）----------
# GITHUB_TOKEN=            # GitHub Search／REST；Actions 內建 secrets.GITHUB_TOKEN
# SCOUT_GITHUB_QUERY=topic:quantitative-finance stars:>20
# SCOUT_SORT=stars         # stars | forks | help-wanted-issues | updated
# SCOUT_PER_PAGE=20        # 1–100
# OSS_README_MAX_CHARS=8000
# OSS_SCOUT_DATE=          # YYYY-MM-DD 覆寫報告日（預設 UTC 今日）
# OSS_WEEKLY_SKIP_TODOS=0  # 1=oss_weekly_pipeline 不寫入 TODOS.md
# OSS_WEEKLY_MAX_WEEKS=8   # TODOS「OSS Scout 週報」區塊保留週數上限

# ---------- PWA / API（選填）----------
# API_HTTP_REQUEST_LOG=0    # 1=true=yes：FastAPI 對 /api/* 記錄 status 與 elapsed_ms（T1b 觀測；預設關）
# PRICE_ALIGNMENT_E2E_OVERRIDES=   # 可選 JSON：{"NVDA":{"ohlc_last_close":100,"quote_last":110}} — 僅供 E2E／staging 強制 snapshot price_alignment（勿用於生產）
# WEB_PUSH_ENABLED=0   # 1=POST /api/push/subscribe 允許（見 docs/PWA_WEB_PUSH.md）
# SYMBOL_SNAPSHOT_HTTP_BASE=   # Streamlit dashboard：Symbol 快照唯讀區改打 HTTP（例 http://127.0.0.1:8000）；未設則 dashboard 以 BigQuery 直建與 GET /api/symbols/{ticker}/snapshot 相同形狀之 payload（symbol_snapshot_service.build_symbol_snapshot）
# DASHBOARD_SYMBOL_FOCUS=BTC   # Streamlit「載入快照」輸入框預設代號（可選；見 dashboard.py）
# EXECUTION_INTENT_STORE=.qsilicon/execution_intents.jsonl  # 執行意圖 JSONL（相對 repo 根）
# PORTFOLIO_HOLDINGS_FILE=portfolio_holdings.jsonl  # Portfolio Tracker v1 JSONL（可設到 .qsilicon/portfolio_holdings.jsonl 或測試 tmp_path）
# PORTFOLIO_STORE_BACKEND=jsonl  # jsonl|bigquery；未設或非 bigquery 時使用 JSONL
# PORTFOLIO_HOLDINGS_TABLE=  # BigQuery backend 用；未設時 /api/portfolio 顯示 pending 且寫入回 503
# SCENARIO_OPTIMIZER_ENABLED=0  # 1=啟用 GET /api/scenario/suggestions（隊列 28d；僅 execution_intents + portfolio 成本口徑之決定性建議，須人工確認、不下單）
# PRICE_ALERTS_FILE=price_alerts.jsonl  # Queue 43 price alert JSONL queue（可設到 .qsilicon/price_alerts.jsonl 或測試 tmp_path）
# RECOMMENDATION_OUTCOMES_TABLE=  # 可選；Track Record nightly mark-to-market BQ sink（見 docs/SQL/recommendation_outcomes.sql）
# EARNINGS_WATCHLIST_OVERRIDE=  # 可選；CSV 覆寫 /api/earnings/upcoming 掃描清單（隊列 45 · P3；空＝預設 MEGA_CAP_TECH_EARNINGS_TICKERS）
# DEEP_FILING_ANALYSIS_FILE=data/deep_filing_analysis.jsonl  # 可選；/api/earnings/{symbol}/insight scaffold 來源；無檔或無對應 ticker 時 endpoint 回 enabled=false（不偽造）；本機試用：cp data/deep_filing_analysis.example.jsonl data/deep_filing_analysis.jsonl（見 docs/EARNINGS_INSIGHT_SCAFFOLD.md）
# COMPUTE_MEMORY_FIXTURE_FILE=data/compute_memory_mock.json  # 可選；GET /api/macro/compute-memory 的 fixture 來源（隊列 45 · P2-mock）；無檔／無效時 endpoint 回 enabled=false
# COMPUTE_MEMORY_LIVE=0  # 1=live 提示位元（需 fixture 自身亦 live=true）；P2-mock 階段預設關閉，未來接 TrendForce／CoreWeave／hyperscaler capex 時用
# ONCHAIN_FIXTURE_FILE=data/onchain_metrics_mock.json  # 可選；GET /api/macro/onchain 的 fixture 來源（隊列 45 · P5-mock）；無檔／無效時 endpoint 回 enabled=false
# ONCHAIN_LIVE=0  # 1=live（需 fixture 自身亦 live=true）；未來接 Glassnode／CryptoQuant／Coinglass 時用
# ONCHAIN_FUNDING_LIVE=0  # 1=啟用 Binance public futures premiumIndex live funding rate；0=固定 mock fixture
ONCHAIN_FUNDING_LIVE=0
# ---------- PWA Web Push（分階；見 docs/PWA_WEB_PUSH.md）----------
# WEB_PUSH_ENABLED=0           # 1=允許 POST /api/push/subscribe（無 Redis 且無 WEB_PUSH_STORE=1 時為 log-only）
# WEB_PUSH_REDIS_URL=          # 例 redis://:pass@host:6379/0 — 訂閱與分散式 rate limit（優先於程序內 store）
# WEB_PUSH_REDIS_SUMMARY_ONLY=0  # 1=Redis 僅存摘要（無法 pywebpush；預設 0=存完整 subscription JSON）
# WEB_PUSH_STORE=0             # 1=程序內暫存（無 REDIS_URL 時）；WEB_PUSH_STORE_FULL_SUBSCRIPTION=1 存完整 JSON 才可發送
# WEB_PUSH_STORE_MAX_SUBSCRIPTIONS=512   # 程序內 store 上限
# WEB_PUSH_SUBSCRIBE_RATE_PER_MIN=30     # client IP 每分鐘 POST 上限（0=關）；有 Redis 時走 Redis INCR
# WEB_PUSH_BQ_PERSIST=0        # 1=每次成功訂閱寫入 BQ（須先建表 docs/SQL/web_push_subscriptions.sql）
# WEB_PUSH_BQ_AUDIT=0         # 1=每次 POST（含 rate_limit）寫入 audit 表
# WEB_PUSH_SUBSCRIPTIONS_TABLE=   # 覆寫預設 {PROJECT}.market_data.web_push_subscriptions（見 config.WEB_PUSH_SUBSCRIPTIONS_TABLE）
# WEB_PUSH_AUDIT_TABLE=         # 覆寫 audit 表 id（預設 …web_push_subscribe_audit）
# WEB_PUSH_ADMIN_KEY=          # 非空時啟用 POST /api/push/test-send（Header: X-Web-Push-Admin-Key）
# WEB_PUSH_VAPID_PRIVATE_KEY=  # PEM（python scripts/vapid_generate.py）
# WEB_PUSH_VAPID_MAILTO=mailto:ops@example.com  # pywebpush vapid sub
# WEB_PUSH_SEND_MAX=50        # broadcast/test-send 最多發送筆數
# ---- 日報 Web Push 投遞（取代 Telegram；見 docs/PWA_WEB_PUSH.md「日報投遞」）----
# 取代 Telegram 日報＝ SKIP_TELEGRAM=1 + WEB_PUSH_DAILY_BRIEF=1。批次 Job 需共享訂閱：
# 必須 WEB_PUSH_ENABLED=1 + WEB_PUSH_REDIS_URL（記憶體 store 在 Cloud Run Job 新 process 為空）。
# WEB_PUSH_DAILY_BRIEF=0      # 1=日報完成後對所有訂閱者推「戰報已更新」通知（固定文案+日期+深連結）
# WEB_PUSH_PORTAL_URL=        # Portal 絕對網址（如 https://your-portal.vercel.app）；組深連結 /report/{date}，空則開 Portal 首頁
# WEB_PUSH_DAILY_SEND_MAX=50  # 日報 broadcast 最多發送筆數（每筆 10s timeout）
# （PWA）VITE_WEB_PUSH_REGISTER=0   # 1=SW 就緒後嘗試 subscribe（須 VITE_WEB_PUSH_VAPID_PUBLIC_KEY）
# （PWA）VITE_WEB_PUSH_VAPID_PUBLIC_KEY=   # URL-safe base64 VAPID public key
# ---------- 實盤價格觀測（BQ vs yfinance；見 scripts/symbol_price_probe.py）----------
# PRICE_PROBE_WRITE_BQ=0      # 1=寫入 PRICE_PROBE_LOG_TABLE
# PRICE_PROBE_LOG_TABLE=      # 例 project.dataset.price_alignment_probe_log（須先建表 docs/SQL/price_probe_log.sql）
# PRICE_PROBE_BQ_METRIC=      # 可選：echo latest_metrics 某欄位名（float），與 yfinance 並列於 stdout JSON
# PAPER_EXECUTION_AUDIT_TABLE= # 可選：隊列 28a；paper tick 或 PATCH /api/execution-intents 成功 append 後寫入稽核列（欄位含 source=paper_tick|http_patch、prev_status）。空則略過。DDL 見 docs/SQL/paper_execution_audit.sql；SKIP_BIGQUERY=1 時仍略過
# （PWA）VITE_TERMINAL_POLL_MS=45000   # Terminal 輪詢間隔（毫秒；見 docs/DASHBOARD_CONTRACT.md）
# （PWA）VITE_TERMINAL_QUERY_COALESCE=1   # 1=多卡同頁時 snapshot/quote/intents 輪詢時間錯開，減少 burst（設 0 關閉）
# （PWA）VITE_TECH_PULSE_URL=   # Tech-Pulse 前端 base URL（Insights 財報外連 /earnings/[ticker]）
# （PWA）VITE_API_URL=   # Cloud Run API base（Vercel 部署必填）
# （PWA）VITE_STRUCTURED_REPORT=0   # 1=報告頁使用 GET /api/reports/{date}/structured 區塊視圖（見 docs/DASHBOARD_CONTRACT.md；預設 0）
# （PWA）VITE_QSILICON_KEY=   # 建置時注入或留空；執行時優先讀 localStorage「qsi_master_key」（`/api-key` 頁寫入）。`data-verification-ui/src/lib/siliconApiHeaders.js` + `useApi.js`／`pushClient.js` 對 `/api/*` fetch 送 Header `X-Q-Silicon-Key`；401 時（`VITE_E2E`≠1）導向 `/api-key?return=…` 並 dispatch `qsilicon:api-unauthorized`
# QSILICON_MASTER_KEY=   # 非空時 FastAPI 對 `/api/*`（`GET /api/stream/war-room` 除外：EventSource 無法帶自訂 header，仍用 `API_STREAM_AUTH_KEY`／query）要求 Header `X-Q-Silicon-Key` 與此值完全一致
# 結構化報告頁可選 URL ?profile=full|lite|crypto-only；後端 GET /api/brief-layouts 列 config/brief_layouts/*.yaml（無 env 開關）

# ---------- NotebookLM / Agency / TradingView（預設關；補充研究，不替代 Gate／客觀工具）----------
# NOTEBOOKLM_ENABLED=0   # 1=允許 LangGraph `deep_filing_analysis_node` 在 filing keywords 命中時呼叫 tools/notebooklm_tool.py；無 notebook id 或無 citation 時不渲染。
# NOTEBOOKLM_NOTEBOOK_ID=   # NotebookLM notebook id；缺失回 [DATA_MISSING:notebooklm_notebook_id_missing]。
# NOTEBOOKLM_COST_LOG_TABLE=   # 可選；非空且未 `SKIP_BIGQUERY` 時寫 NotebookLM 查詢成本／狀態 row。DDL 見 docs/SQL/notebooklm_cost_log.sql。
# AGENCY_RESEARCH_ENABLED=0   # 1=允許 agents.agency 載入 template，並在 LangGraph `agency_researcher_node`／Crew backstory 中作補充；0 時主線無行為變更。
# TRADINGVIEW_MCP_ENABLED=0   # 1=允許 tools/tradingview.py 執行 repo-side command bridge；預設關。
# TRADINGVIEW_MCP_COMMAND=   # 例：python scripts/tradingview_mcp_bridge.py；repo 僅讀此 command，不修改 ~/.claude 或安裝外部 MCP server。
# TRADINGVIEW_TIMEOUT_SEC=8
# TRADINGVIEW_FALLBACK_YFINANCE=1   # bridge 未啟用或失敗時，以 multi_timeframe_tool/yfinance 靜默 fallback；設 0 則回 DATA_MISSING。

# ---------- Runbook 視覺化（visualizer.py；見 docs/architecture/visualization_plan.md 階段 C）----------
# VISUALIZER_BTC_SOURCE=yfinance   # yfinance（預設）｜snapshot — Panel 1 BTC 收盤序列改由 BQ + symbol_snapshot_service 之 price_series（須 GCP/BQ 可用）

# TERMINAL_SSE_ENABLED=0       # 1=啟用 GET /api/stream/war-room（SSE；見 docs/TERMINAL_MID_TIER_ROADMAP.md M4）
# API_STREAM_AUTH_KEY=         # 非空時 SSE 須 Header X-QS-Stream-Key／query stream_key，或下方短期 token
# SSE_TOKEN_TTL_SECONDS=60     # POST /api/stream/token 鑄出短期 token 的 TTL（10–600 clamp；需 API_STREAM_AUTH_KEY 開啟；瀏覽器以 stream_token=...）
# SSE_MAX_EVENTS_PER_SEC=0     # >0 啟用每連線事件節流；超過後 yield 一則 event: throttled（0–100 clamp；: keepalive 不計）
# TERMINAL_SSE_POLL_SEC=2      # SSE 迴圈 sleep 秒數（0.5–30）
# PAPER_TICK_HTTP_ENABLED=0    # 1=啟用 POST /api/paper/execution-tick（紙上模擬一輪；見 M5）
# PAPER_TICK_API_KEY=          # 非空時上述 POST 須 Header X-Paper-Tick-Key
# --- GitHub Actions 自動排程（.github/workflows/paper-execution-tick.yml） ---
# Secrets 設定於 GitHub → Settings → Secrets and variables → Actions：
#   GCP_SA_KEY                  已有（與主流程共用）
#   PAPER_EXECUTION_AUDIT_TABLE 可選：project.dataset.paper_execution_audit（BQ 稽核；DDL 見 docs/SQL/paper_execution_audit.sql）

# ---------- Push Alert Digest（M4；.github/workflows/push-digest-tick.yml；每 30 分鐘） ----------
# PRICE_ALERTS_TELEGRAM_ENABLED=0  # 1=check_price_alerts 命中後額外送 Telegram；自然去重（alert.triggered_at 永久標記）
# PRICE_ALERTS_FILE=.qsilicon/price_alerts.jsonl  # alert 條件 JSONL（PWA 透過 /api/push/price-alerts 建立）
# 排程使用既有 TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID。
# SSE_PRICE_ALERT_ENABLED=0  # 1=命中後同時 publish 到 /api/stream/war-room 的 event: price_alert（PWA 即時 toast；bounded deque maxlen=64）
