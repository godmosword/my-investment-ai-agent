# Historical Ruff default. Unpinned ruff 0.16+ reports ~663 extra rules
# (BLE / I / DTZ / PERF / …) and keeps CI `quick` red (CI-QUICK-BASELINE).
# New work still must pass E4 / E7 / E9 / F (`ruff check .`).
[lint]
select = ["E4", "E7", "E9", "F"]
