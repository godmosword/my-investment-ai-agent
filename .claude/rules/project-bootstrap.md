# Project bootstrap (read `CLAUDE.md`)

**Claude Code:** The repository root **`CLAUDE.md`** is the primary developer guide. Load or read it when:

- You **start work** in this project or resume after context loss
- The task touches **pipeline**, **crew**, **tools**, **validators**, **BigQuery**, or **deploy**

It covers: red lines (no fabricated market data; Telegram HTML whitelist), module/file map, common commands (`ruff`, `pytest -m smoke`), `docs/` index, observability (`gate_failure_log`, scratchpad), and links to `README.md`, `TODOS.md`, `CHANGELOG.md`.

Do not skip it for multi-file refactors or new features unless you already have `CLAUDE.md` in context from this session.
