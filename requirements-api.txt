# HTTP API only (Vercel FastAPI service / uvicorn api:app).
# Do not install the Job requirements.txt here — that image is far heavier
# (orchestration stack, dashboard, embeddings, GCP clients).
fastapi>=0.111.0
pydantic>=2.7.0
python-multipart>=0.0.9
Jinja2>=3.1.0
MarkupSafe>=2.1.0
PyYAML>=6.0.1
requests>=2.32.0
yfinance>=0.2.50
httpx>=0.27.0
