---
description: Agent 編排 — 僅在 /agent-plan、/agent-action 時啟用（勿 alwaysApply）
alwaysApply: false
---

# Agent Orchestration（Q-Silicon）

> **啟用方式：** 使用者輸入 **`/agent-plan`** 或 **`/agent-action`** 時才依本規則與指令檔執行；一般對話不自動進入 Leader 編排模式。

完整說明：[`docs/AGENT-WORKFLOW.md`](../../docs/AGENT-WORKFLOW.md)。

## 入口指令

| 指令 | 做什麼 | 不做什麼 |
|------|--------|----------|
| **`/agent-plan`** | Leader 寫 Plan → **架構 + 工程並行審核** → Approved Plan | 預設不實作、不 commit |
| **`/agent-action`** | 讀 Approved Plan → **Task 派工** → 整合 → Verify →（可選）Ship | 不重做完整 Plan（blocked 除外） |

## 紅線（摘要）

無數據幻覺 · Telegram HTML 白名單 · Tool `_get_cache`/`_set_cache` · `main.py` 雙線程安全 · Gate／`validate_report` · 戰報非教學腔

## Leader

當前 session 主模型（Cursor 常為 Composer）：Plan、拆任務、整合、git scope。

## 模型 slug（Task `model`）

| 用途 | slug |
|------|------|
| Leader | `composer-2.5-fast` |
| Plan 架構審 / L3 | `claude-opus-4-8-thinking-medium` |
| Plan 工程審 / diff review | `gpt-5.5-medium` |
| L2 / 戰報文案 | `claude-4.6-sonnet-medium-thinking` |
| explore | `grok-4.3` |
| shell / 批次 | `grok-build-0.1` |
| 備選 Plan 審 | `claude-fable-5-thinking-medium` |

**勿用** haiku／gpt-5.4 改核心金融路徑。

## `/agent-action` 路由

- L3 → Leader 或 Opus | L2 → Sonnet 4.6 | L1 → explore + Leader | L0 → `shell`
- Graph／crew → 必跑 `scripts/verify_graph_gate.sh`
- Portal → lint + e2e
- 可並行不同檔；**禁止**多 agent 同檔
- commit → Leader only；**只 stage 相關檔**；預設不 push

## Verify（觸及面）

`ruff` + `pytest -m smoke` · graph gate · PWA lint/e2e · `ci_terminal_contract_check.sh` — 見 AGENT-WORKFLOW 矩陣

## CRITICAL

A/B/C 格式 — 見 `review-user-choice.mdc`

## 跳過 `/agent-plan`

單檔小改、一條命令、使用者說「直接做」→ 可只 `/agent-action`。
