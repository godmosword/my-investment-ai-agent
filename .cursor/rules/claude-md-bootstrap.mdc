---
description: 接手開發或處理本專案任務時，先讀 CLAUDE.md 建立共同上下文
alwaysApply: true
---

# 開發前：先讀 `CLAUDE.md`

在**第一次**處理本 repo、**切換大任務**、或對架構／檔案分工不熟時：

1. **先讀** 專案根目錄的 **`CLAUDE.md`**（開發者導覽：紅線、模組表、`docs/` 索引、常用指令、觀測與 Gate）。
2. 細節與環境變數以 **`README.md`**、**`ENV_TEMPLATE.txt`** 為準。
3. 待辦與完成度以 **`TODOS.md`** 為準。

不必每則使用者訊息都重讀；但在**開始寫程式或改管線前**若尚未讀過本輪對話，應載入 `CLAUDE.md` 以免遺漏專案紅線與檔案地圖。
