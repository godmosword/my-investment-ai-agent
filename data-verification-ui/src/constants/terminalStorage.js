/** localStorage CSV for SSE ``watch_symbols`` (War Room). */
export const TERMINAL_SSE_WATCH_KEY = "terminal_sse_watch";

/** localStorage CSV for Command Bar recent symbols (newest first, max 8). */
export const TERMINAL_RECENT_SYMBOLS_KEY = "terminal_recent_symbols";

/** Dispatched after ``terminal_sse_watch`` is written (same-tab listeners). */
export const TERMINAL_SSE_WATCH_CHANGED_EVENT = "terminal_sse_watch_changed";
